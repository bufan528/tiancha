/** Analyst agent -Phase 2 placeholder. Contract only. */
import type { AgentRole } from "../domain/index.js";
export interface AnalystAgent { readonly role: AgentRole }
export const analystAgentRole: AgentRole = "analyst";
